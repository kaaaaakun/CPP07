#ifndef ITER_HPP
#define ITER_HPP

#include <iostream>

template <typename T>
void iter(T *arrayPtr, int arraySize, void (*foo)(const T)) {
	for(int i = 0; i<size; i++;) {
		foo(arr[i]);
	}
}

template <typename T>
void iter(T *arrayPtr, int arraySize, void (*foo)(const T)) {
	for(int i = 0; i<size; i++;) {
		foo(arr[i]);
	}
}

#endif
